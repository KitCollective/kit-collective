# Football Data Seed succeeds KitCollective Seed

The git repo stays one remote with `seed/` inside it (ADR-0003). Linear still splits product vs seed (ADR-0004): two projects, not three and not one. The seed board is now **Football Data Seed**. KitCollective Seed is the predecessor and is closed when `/to-spec` creates the successor. Fetch tickets stay off the KitCollective product board.

Status: accepted.
