# Cross MCP chat is seed_grain and seed_join on kc_seed_mcp HTTP

Predecessor `kc_seed_mcp` stdio exposes `seed_apify` and `seed_fk` only. Cross MCP accept keeps that Cursor server id and speaks Streamable HTTP with `seed_grain` (one Hierarchy grain) and `seed_join` (Join workflow; FK after facts inside). Stdio stays an in-tree debug binary, not the accept. A new Cursor id, or `seed_fk` as a chat sibling, would fork the catalog.

Status: accepted.
