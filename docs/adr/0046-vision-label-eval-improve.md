# Vision Matcher learns on Nest artifacts, not Gemini weights

Collector Save is the **Vision label**: snapshot suggested versus selected Club xor NationalTeam, season, type, catalogKitId, player, and patch on the identity job. Nest scores it at Save (**Vision eval**: per-field hit, `user_action`, class `accepted` | `alias` | `coverage` | `model` | `transport`). **Vision improve** is a Staff-gated Postgres row in the Admin Data table (CatalogLabel alias, prompt, or seed) — never an auto-write of stamdata, never a second Gemini call per Save, never Vertex fine-tune in this increment, never CLIP/pgvector on the collector path. OpenRouter/Gemini stays the Vision suggestion provider (ADR-0044). Google paid Vision does not train on these photos; the loop that gets stronger is KitCollective’s mapper and catalog. Grouping labels wait. Factory eval-agent aggregates; Staff applies.

Status: accepted.
