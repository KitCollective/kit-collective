# Signal-up files into Linear Triage

Out-of-scope findings used to land in Backlog. Planner cannot claim `signal-up`, so they sat on the board next to real slices. New `signal-up` and `proposal` issues are created in Linear **Triage** (the state, not the label group). Planner still never claims Triage and never claims `signal-up`.

Status: accepted
