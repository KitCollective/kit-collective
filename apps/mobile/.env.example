# Expo public API base URL (no secrets). Set per EAS channel in GitHub Environment.
EXPO_PUBLIC_API_URL=http://localhost:3000

# Local Desktop Metro only. When "off", Tilføj skips the Plus paywall.
# Release/EAS builds ignore this even if set. Do not set on EAS channels.
# EXPO_PUBLIC_ENTITLEMENT_GATE=off
