---
name: area-expo
description: Area skill for Expo / React Native. Load on demand from the Frontend or DevOps Role when the slice touches apps/mobile, Expo Router, EAS, or Expo Web. Not an agent id.
---

# Area skill — Expo

This is an **Area skill**, not a Role subagent. Implement lead does not spawn an agent named Expo.

Load from Frontend (mobile UI) or DevOps (EAS only):

1. `.cursor/skills/expo/expo-overview/SKILL.md` first
2. Then the matching leaf under `.cursor/skills/expo/` (router, native-ui, data-fetching, animation, upgrade, eas-*)

Product docs (`CONTEXT.md`, ADRs, `docs/design-system.md`) win on conflict with vendor Expo defaults.
