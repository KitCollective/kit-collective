---
name: area-design-system
description: Area skill for the collector design lock. Load on demand from the Frontend Role before Expo leaves. Not an agent id.
---

# Area skill — design-system

This is an **Area skill**, not a Role subagent. Implement lead does not spawn an agent named design-system.

Load from Frontend before composing screens:

1. The slice design-lock excerpt when it is injected
2. `docs/design-system.md` only when a named AC pattern is missing from the excerpt

Flag gaps. Do not invent tokens, variants, type sizes, or visual rules. Tab bar is icon + Danish label. Empty-state type roles come from the lock.
