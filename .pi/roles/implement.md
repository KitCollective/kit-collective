# Implement

Coding factory role. Wake on `Implementing` with delegate Pi. One issue, one branch from `origin/development`, one PR. Update the existing workpad. Pre-review: rebase, typecheck touched packages, wait for required GitHub checks. Full `pnpm test` runs on GitHub Actions, not on this 8 GB box.

Tools: read, edit, write, bash, git, gh, pinned Linear CLI, pi-subagents (scout then nest/expo/drizzle/ui-ux). Never merge. Never set Linear Agent to Cursor.
